# CLAUDE.md

## 最高指導原則（Top Guiding Principles）

> 以下規則優先於本檔案其他所有內容，也優先於任何預設行為。

### 1. 模型分工：Opus 規劃與審查，Sonnet 執行

當使用者提出需求時，一律採用以下流程：

1. **分析與規劃（Opus）**：由 Opus 模型理解需求、勘查程式碼、決定作法，並產出明確的執行計畫（要改哪些檔案、每一步要做什麼、驗收標準）。
2. **實際執行（Sonnet）**：實作工作交由 **Sonnet** 模型進行，方式是使用 Agent 工具並指定 `model: "sonnet"`，把上一步的計畫當作任務說明傳給它。計畫要夠具體，讓 Sonnet 不需再自行揣測需求。
3. **最後審查（Opus）**：由 Opus 模型審查 Sonnet 的產出——檢查差異（diff）、是否符合原始需求、是否有錯誤或遺漏、是否有多餘變更。有問題就退回修正，通過後才回報完成。

補充說明：

- 規劃階段與審查階段**不得**下放給 Sonnet；執行階段除非使用者另有指示，否則**不由 Opus 直接動手**。
- 若使用者明確指定要用某個模型，或明確要求直接執行，則以使用者當下的指示為準。
- 純粹的問答、查詢、極小幅度的修改（例如單純回答問題、讀檔說明），可由 Opus 直接回覆，不必啟動三段式流程。
- 每個階段都要讓使用者知道目前處於哪一段（規劃／執行／審查）。

### 2. 複雜計畫一律以 sub-agent 執行

當規劃出來的計畫較為複雜時，**必須**把執行工作拆給 sub-agent（Agent 工具，`model: "sonnet"`）進行，不可由主線直接一路做完。

判斷為「複雜」的情形（符合任一項即是）：

- 需要修改多個檔案，或牽涉多個模組／頁面。
- 步驟超過三個，或包含彼此相依的階段。
- 需要大量勘查、比對、搜尋才能決定作法。
- 同時包含實作與驗證（例如改完還要跑起來看結果）。

執行方式：

- 計畫可切成互相獨立的區塊時，就派多個 sub-agent 平行處理，各自負責明確、不重疊的範圍。
- 每個 sub-agent 都要收到完整且自足的任務說明：目標、要碰哪些檔案、限制條件、完成標準。sub-agent 從零開始，沒有本次對話的上下文。
- sub-agent 回報後，仍依原則 1 由 Opus 做最後審查；不可把 sub-agent 的回報直接當成完成。

簡單、單一檔案、一兩步就結束的工作，不需要動用 sub-agent。
